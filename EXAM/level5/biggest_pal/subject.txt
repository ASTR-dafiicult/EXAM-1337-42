Assignment name  : biggest_pal
Expected files   : *.c, *.h
Allowed functions: write
--------------------------------------------------------------------------------

Write a program named biggest_pal that takes a string as argument and prints the
biggest palindrome included inside this string, followed by a newline.

This string contains only lowercase characters.

If there is two palindromes of the same length, you have to print the last one.

If there is more or less than 1 argument, your program must print only a newline
only.

Exemples:

$> biggest_pal "abcba"
abcba
$> biggest_pal "aaaaaaaaaabbcbbaaaa"
aaaabbcbbaaaa
$> biggest_pal "aooibdaoiwhoihwdoinzeldaisaboyobasiadlezfdsfnslk"
zeldaisaboyobasiadlez
$> biggest_pal "aeibaabaammaabaalek"
aabaammaabaa
$> biggest_pal abeb qpinqwjobo qkmnwoiq

$> biggest_pal

$>


-----------code V1----------
Analysis Result
The provided code aims to find the longest palindromic substring within a given string. Here's an analysis of its time and space complexity:

Time Complexity:
- The main function, biggest_pal, calculates the length of the string in O(n) time, where n is the length of the input string.
- The brute_force function iterates over each character in the string (outer loop), which is O(n).
- For each position, it considers substrings starting at that position and expanding outward, which can be up to O(n) in the worst case.
- For each substring, it calls check_palindrom, which compares characters from both ends inward, taking O(k) time where k is the length of the substring.
- Since the nested loops generate all possible substrings, the total number of substrings is approximately O(n^2).
- For each substring, the palindrome check can take up to O(n) in the worst case.
- Therefore, the overall time complexity is approximately O(n^3).

Space Complexity:
- The main additional space used is for the output string (tab), which can be up to O(n) in length.
- The check_palindrom function uses a few integer variables, which are O(1) space.
- No additional significant data structures are used.
- Overall, the space complexity is O(n).

In summary:
- Time complexity: O(n^3)
- Space complexity: O(n)
----------------------------



-----------code V2:-------------------
Analysis Result
The provided code implements a function to find the longest palindromic substring using a center expansion approach.

Time Complexity:
- The main loop iterates over each character in the string (size n), so O(n).
- For each character, the code expands around the center to find the longest palindrome, which in the worst case can take O(n) time (if the palindrome is nearly the entire string).
- Therefore, the overall time complexity is O(n^2).

Space Complexity:
- The function allocates a result buffer of size proportional to the input string (O(n)).
- Additional variables use constant space.
- The recursive or iterative expansion does not require extra space proportional to n.
- Hence, the space complexity is O(n) due to the allocated result buffer.

In summary:
Time complexity: O(n^2)
Space complexity: O(n)
-----------------------------------------